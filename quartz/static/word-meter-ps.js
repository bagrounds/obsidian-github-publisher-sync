/* Generated by Spago v1.0.4 */
(() => {
  // output/Control.Semigroupoid/index.js
  var semigroupoidFn = {
    compose: function(f) {
      return function(g) {
        return function(x) {
          return f(g(x));
        };
      };
    }
  };

  // output/Control.Category/index.js
  var identity = function(dict) {
    return dict.identity;
  };
  var categoryFn = {
    identity: function(x) {
      return x;
    },
    Semigroupoid0: function() {
      return semigroupoidFn;
    }
  };

  // output/Data.Function/index.js
  var $$const = function(a) {
    return function(v) {
      return a;
    };
  };

  // output/Data.Unit/foreign.js
  var unit = void 0;

  // output/Data.Functor/index.js
  var map = function(dict) {
    return dict.map;
  };

  // output/Data.Semigroup/index.js
  var append = function(dict) {
    return dict.append;
  };

  // output/Control.Apply/index.js
  var identity2 = /* @__PURE__ */ identity(categoryFn);
  var apply = function(dict) {
    return dict.apply;
  };
  var applySecond = function(dictApply) {
    var apply1 = apply(dictApply);
    var map2 = map(dictApply.Functor0());
    return function(a) {
      return function(b) {
        return apply1(map2($$const(identity2))(a))(b);
      };
    };
  };

  // output/Control.Applicative/index.js
  var pure = function(dict) {
    return dict.pure;
  };
  var liftA1 = function(dictApplicative) {
    var apply2 = apply(dictApplicative.Apply0());
    var pure1 = pure(dictApplicative);
    return function(f) {
      return function(a) {
        return apply2(pure1(f))(a);
      };
    };
  };

  // output/Data.Bounded/foreign.js
  var topChar = String.fromCharCode(65535);
  var bottomChar = String.fromCharCode(0);
  var topNumber = Number.POSITIVE_INFINITY;
  var bottomNumber = Number.NEGATIVE_INFINITY;

  // output/Data.Show/foreign.js
  var showIntImpl = function(n) {
    return n.toString();
  };

  // output/Data.Show/index.js
  var showInt = {
    show: showIntImpl
  };
  var show = function(dict) {
    return dict.show;
  };

  // output/Data.Maybe/index.js
  var Nothing = /* @__PURE__ */ function() {
    function Nothing2() {
    }
    ;
    Nothing2.value = new Nothing2();
    return Nothing2;
  }();
  var Just = /* @__PURE__ */ function() {
    function Just2(value0) {
      this.value0 = value0;
    }
    ;
    Just2.create = function(value0) {
      return new Just2(value0);
    };
    return Just2;
  }();

  // output/WordMeter.Vdom/foreign.js
  var findElementByIdImpl = (nothing) => (just) => (id) => () => {
    if (typeof document === "undefined") return nothing;
    const found = document.getElementById(id);
    return found === null ? nothing : just(found);
  };
  var createElementInDocument = (tag) => () => document.createElement(tag);
  var createTextNodeInDocument = (value) => () => document.createTextNode(value);
  var setAttributeOnElement = (name) => (value) => (node) => () => {
    node.setAttribute(name, value);
  };
  var setStyleOnElement = (property) => (value) => (node) => () => {
    node.style.setProperty(property, value);
  };
  var attachClickListener = (node) => (handler) => () => {
    node.addEventListener("click", () => handler());
  };
  var appendChildToElement = (parent) => (child) => () => {
    parent.appendChild(child);
  };
  var removeAllChildrenFromElement = (node) => () => {
    while (node.firstChild) node.removeChild(node.firstChild);
  };

  // output/Control.Bind/foreign.js
  var arrayBind = typeof Array.prototype.flatMap === "function" ? function(arr) {
    return function(f) {
      return arr.flatMap(f);
    };
  } : function(arr) {
    return function(f) {
      var result = [];
      var l = arr.length;
      for (var i = 0; i < l; i++) {
        var xs = f(arr[i]);
        var k = xs.length;
        for (var j = 0; j < k; j++) {
          result.push(xs[j]);
        }
      }
      return result;
    };
  };

  // output/Control.Bind/index.js
  var bind = function(dict) {
    return dict.bind;
  };

  // output/Data.Foldable/foreign.js
  var foldrArray = function(f) {
    return function(init) {
      return function(xs) {
        var acc = init;
        var len = xs.length;
        for (var i = len - 1; i >= 0; i--) {
          acc = f(xs[i])(acc);
        }
        return acc;
      };
    };
  };
  var foldlArray = function(f) {
    return function(init) {
      return function(xs) {
        var acc = init;
        var len = xs.length;
        for (var i = 0; i < len; i++) {
          acc = f(acc)(xs[i]);
        }
        return acc;
      };
    };
  };

  // output/Data.Monoid/index.js
  var mempty = function(dict) {
    return dict.mempty;
  };

  // output/Data.Foldable/index.js
  var foldr = function(dict) {
    return dict.foldr;
  };
  var traverse_ = function(dictApplicative) {
    var applySecond2 = applySecond(dictApplicative.Apply0());
    var pure3 = pure(dictApplicative);
    return function(dictFoldable) {
      var foldr2 = foldr(dictFoldable);
      return function(f) {
        return foldr2(function($454) {
          return applySecond2(f($454));
        })(pure3(unit));
      };
    };
  };
  var foldMapDefaultR = function(dictFoldable) {
    var foldr2 = foldr(dictFoldable);
    return function(dictMonoid) {
      var append2 = append(dictMonoid.Semigroup0());
      var mempty2 = mempty(dictMonoid);
      return function(f) {
        return foldr2(function(x) {
          return function(acc) {
            return append2(f(x))(acc);
          };
        })(mempty2);
      };
    };
  };
  var foldableArray = {
    foldr: foldrArray,
    foldl: foldlArray,
    foldMap: function(dictMonoid) {
      return foldMapDefaultR(foldableArray)(dictMonoid);
    }
  };

  // output/Effect/foreign.js
  var pureE = function(a) {
    return function() {
      return a;
    };
  };
  var bindE = function(a) {
    return function(f) {
      return function() {
        return f(a())();
      };
    };
  };

  // output/Control.Monad/index.js
  var ap = function(dictMonad) {
    var bind3 = bind(dictMonad.Bind1());
    var pure3 = pure(dictMonad.Applicative0());
    return function(f) {
      return function(a) {
        return bind3(f)(function(f$prime) {
          return bind3(a)(function(a$prime) {
            return pure3(f$prime(a$prime));
          });
        });
      };
    };
  };

  // output/Effect/index.js
  var $runtime_lazy = function(name, moduleName, init) {
    var state = 0;
    var val;
    return function(lineNumber) {
      if (state === 2) return val;
      if (state === 1) throw new ReferenceError(name + " was needed before it finished initializing (module " + moduleName + ", line " + lineNumber + ")", moduleName, lineNumber);
      state = 1;
      val = init();
      state = 2;
      return val;
    };
  };
  var monadEffect = {
    Applicative0: function() {
      return applicativeEffect;
    },
    Bind1: function() {
      return bindEffect;
    }
  };
  var bindEffect = {
    bind: bindE,
    Apply0: function() {
      return $lazy_applyEffect(0);
    }
  };
  var applicativeEffect = {
    pure: pureE,
    Apply0: function() {
      return $lazy_applyEffect(0);
    }
  };
  var $lazy_functorEffect = /* @__PURE__ */ $runtime_lazy("functorEffect", "Effect", function() {
    return {
      map: liftA1(applicativeEffect)
    };
  });
  var $lazy_applyEffect = /* @__PURE__ */ $runtime_lazy("applyEffect", "Effect", function() {
    return {
      apply: ap(monadEffect),
      Functor0: function() {
        return $lazy_functorEffect(0);
      }
    };
  });

  // output/WordMeter.Vdom/index.js
  var bind2 = /* @__PURE__ */ bind(bindEffect);
  var traverse_2 = /* @__PURE__ */ traverse_(applicativeEffect)(foldableArray);
  var ElementNode = /* @__PURE__ */ function() {
    function ElementNode2(value0) {
      this.value0 = value0;
    }
    ;
    ElementNode2.create = function(value0) {
      return new ElementNode2(value0);
    };
    return ElementNode2;
  }();
  var TextNode = /* @__PURE__ */ function() {
    function TextNode2(value0) {
      this.value0 = value0;
    }
    ;
    TextNode2.create = function(value0) {
      return new TextNode2(value0);
    };
    return TextNode2;
  }();
  var text = /* @__PURE__ */ function() {
    return TextNode.create;
  }();
  var style = function(property) {
    return function(value) {
      return {
        property,
        value
      };
    };
  };
  var onClick = function(handler) {
    return {
      eventName: "click",
      handler
    };
  };
  var element = function(tag) {
    return function(attributes) {
      return function(styles) {
        return function(listeners) {
          return function(children) {
            return new ElementNode({
              tag,
              attributes,
              styles,
              listeners,
              children
            });
          };
        };
      };
    };
  };
  var span_ = function(attributes) {
    return function(styles) {
      return function(children) {
        return element("span")(attributes)(styles)([])(children);
      };
    };
  };
  var div_ = function(attributes) {
    return function(styles) {
      return function(children) {
        return element("div")(attributes)(styles)([])(children);
      };
    };
  };
  var button = /* @__PURE__ */ element("button");
  var attribute = function(name) {
    return function(value) {
      return {
        name,
        value
      };
    };
  };
  var buttonType = /* @__PURE__ */ attribute("type");
  var testId = /* @__PURE__ */ attribute("data-testid");
  var applyStyle = function(node) {
    return function(decl) {
      return setStyleOnElement(decl.property)(decl.value)(node);
    };
  };
  var applyListener = function(node) {
    return function(listener) {
      return attachClickListener(node)(listener.handler);
    };
  };
  var applyAttribute = function(node) {
    return function(attr) {
      return setAttributeOnElement(attr.name)(attr.value)(node);
    };
  };
  var renderNode = function(v) {
    if (v instanceof TextNode) {
      return createTextNodeInDocument(v.value0);
    }
    ;
    if (v instanceof ElementNode) {
      return function __do2() {
        var node = createElementInDocument(v.value0.tag)();
        traverse_2(applyAttribute(node))(v.value0.attributes)();
        traverse_2(applyStyle(node))(v.value0.styles)();
        traverse_2(applyListener(node))(v.value0.listeners)();
        traverse_2(appendRenderedChild(node))(v.value0.children)();
        return node;
      };
    }
    ;
    throw new Error("Failed pattern match at WordMeter.Vdom (line 97, column 1 - line 97, column 37): " + [v.constructor.name]);
  };
  var appendRenderedChild = function(parent) {
    return function(child) {
      return bind2(renderNode(child))(appendChildToElement(parent));
    };
  };
  var mount = function(hostId) {
    return function(tree) {
      return function __do2() {
        var hostMaybe = findElementByIdImpl(Nothing.value)(Just.create)(hostId)();
        if (hostMaybe instanceof Nothing) {
          return unit;
        }
        ;
        if (hostMaybe instanceof Just) {
          removeAllChildrenFromElement(hostMaybe.value0)();
          var child = renderNode(tree)();
          return appendChildToElement(hostMaybe.value0)(child)();
        }
        ;
        throw new Error("Failed pattern match at WordMeter.Vdom (line 90, column 3 - line 95, column 38): " + [hostMaybe.constructor.name]);
      };
    };
  };

  // output/WordMeter.Version/index.js
  var version = "0.1.0";

  // output/Data.Array/foreign.js
  var replicateFill = function(count, value) {
    if (count < 1) {
      return [];
    }
    var result = new Array(count);
    return result.fill(value);
  };
  var replicatePolyfill = function(count, value) {
    var result = [];
    var n = 0;
    for (var i = 0; i < count; i++) {
      result[n++] = value;
    }
    return result;
  };
  var replicateImpl = typeof Array.prototype.fill === "function" ? replicateFill : replicatePolyfill;
  var length = function(xs) {
    return xs.length;
  };
  var filterImpl = function(f, xs) {
    return xs.filter(f);
  };

  // output/Data.Function.Uncurried/foreign.js
  var runFn2 = function(fn) {
    return function(a) {
      return function(b) {
        return fn(a, b);
      };
    };
  };

  // output/Data.Array/index.js
  var filter = /* @__PURE__ */ runFn2(filterImpl);

  // output/Data.String.Common/foreign.js
  var replaceAll = function(s1) {
    return function(s2) {
      return function(s3) {
        return s3.replace(new RegExp(s1.replace(/[-\/\\^$*+?.()|[\]{}]/g, "\\$&"), "g"), s2);
      };
    };
  };
  var split = function(sep) {
    return function(s) {
      return s.split(sep);
    };
  };
  var trim = function(s) {
    return s.trim();
  };

  // output/WordMeter.Words/index.js
  var isNonEmpty = function(value) {
    return value !== "";
  };
  var collapseWhitespaceToSpace = /* @__PURE__ */ function() {
    var $2 = replaceAll("\r")(" ");
    var $3 = replaceAll("\n")(" ");
    var $4 = replaceAll("	")(" ");
    return function($5) {
      return $2($3($4($5)));
    };
  }();
  var countWords = function(transcript) {
    return length(filter(isNonEmpty)(split(" ")(collapseWhitespaceToSpace(trim(transcript)))));
  };

  // output/WordMeter.Recording/index.js
  var show2 = /* @__PURE__ */ show(showInt);
  var Toggle = /* @__PURE__ */ function() {
    function Toggle2() {
    }
    ;
    Toggle2.value = new Toggle2();
    return Toggle2;
  }();
  var InjectFinalTranscript = /* @__PURE__ */ function() {
    function InjectFinalTranscript2(value0) {
      this.value0 = value0;
    }
    ;
    InjectFinalTranscript2.create = function(value0) {
      return new InjectFinalTranscript2(value0);
    };
    return InjectFinalTranscript2;
  }();
  var reduce = function(v) {
    return function(v1) {
      if (v instanceof Toggle) {
        return {
          totalWords: v1.totalWords,
          lastError: v1.lastError,
          listening: !v1.listening
        };
      }
      ;
      if (v instanceof InjectFinalTranscript) {
        if (v1.listening) {
          return {
            listening: v1.listening,
            lastError: v1.lastError,
            totalWords: v1.totalWords + countWords(v.value0) | 0
          };
        }
        ;
        return v1;
      }
      ;
      throw new Error("Failed pattern match at WordMeter.Recording (line 33, column 1 - line 33, column 39): " + [v.constructor.name, v1.constructor.name]);
    };
  };
  var initialSession = /* @__PURE__ */ function() {
    return {
      listening: false,
      totalWords: 0,
      lastError: Nothing.value
    };
  }();
  var buildVersion = /* @__PURE__ */ function() {
    return span_([testId("wm-version")])([style("display")("block"), style("margin-top")("12px"), style("font-size")("11px"), style("color")("#7d8590")])([text("Word Meter (PureScript) v" + version)]);
  }();
  var buildToggle = function(send) {
    return function(session) {
      return button([testId("wm-toggle"), buttonType("button")])([style("margin-top")("12px"), style("padding")("10px 18px"), style("border")("0"), style("border-radius")("999px"), style("background")(function() {
        if (session.listening) {
          return "#a85f5f";
        }
        ;
        return "#1f6feb";
      }()), style("color")("white"), style("cursor")("pointer"), style("font")("inherit")])([onClick(send(Toggle.value))])([text(function() {
        if (session.listening) {
          return "Stop counting";
        }
        ;
        return "Start counting";
      }())]);
    };
  };
  var buildTag = /* @__PURE__ */ div_([/* @__PURE__ */ testId("wm-build")])([/* @__PURE__ */ style("font-size")("12px"), /* @__PURE__ */ style("letter-spacing")("0.08em"), /* @__PURE__ */ style("text-transform")("uppercase"), /* @__PURE__ */ style("color")("#7aa2f7")])([/* @__PURE__ */ text("PureScript build")]);
  var buildStatus = function(session) {
    return div_([testId("wm-status")])([style("font-size")("13px"), style("color")("#9aa5b1"), style("margin")("6px 0 4px")])([text(function() {
      if (session.listening) {
        return "Listening";
      }
      ;
      return "Idle";
    }())]);
  };
  var buildCountLabel = /* @__PURE__ */ div_([/* @__PURE__ */ testId("wm-count-label")])([/* @__PURE__ */ style("font-size")("14px"), /* @__PURE__ */ style("color")("#9aa5b1")])([/* @__PURE__ */ text("words counted")]);
  var buildCount = function(session) {
    return div_([testId("wm-count")])([style("font-size")("48px"), style("font-variant-numeric")("tabular-nums"), style("margin")("4px 0")])([text(show2(session.totalWords))]);
  };
  var view = function(send) {
    return function(session) {
      return div_([testId("wm-root")])([style("font-family")("system-ui, -apple-system, sans-serif"), style("padding")("16px"), style("border-radius")("12px"), style("background")("#0b1220"), style("color")("#e6edf3"), style("max-width")("420px")])([buildTag, buildStatus(session), buildCount(session), buildCountLabel, buildToggle(send)(session), buildVersion]);
    };
  };

  // output/WordMeter.State/foreign.js
  var newCell = (initial) => () => ({ value: initial });
  var readCell = (cell) => () => cell.value;
  var writeCell = (value) => (cell) => () => {
    cell.value = value;
  };

  // output/WordMeter.State/index.js
  var read3 = readCell;
  var $$new2 = newCell;
  var modify3 = function(f) {
    return function(cell) {
      return function __do2() {
        var current = readCell(cell)();
        return writeCell(f(current))(cell)();
      };
    };
  };

  // output/WordMeter.TestHook/foreign.js
  var installTestHookImpl = (api) => () => {
    if (typeof window === "undefined") return;
    window.__wordMeter = {
      simulateFinalTranscript: (transcript) => api.simulateFinalTranscript(transcript)(),
      start: () => api.start(),
      stop: () => api.stop(),
      getTotalWords: () => api.getTotalWords(),
      getListening: () => api.getListening(),
      getVersion: () => api.getVersion()
    };
  };

  // output/WordMeter.TestHook/index.js
  var pure2 = /* @__PURE__ */ pure(applicativeEffect);
  var install = function(v) {
    return installTestHookImpl({
      simulateFinalTranscript: function(transcript) {
        return v.send(new InjectFinalTranscript(transcript));
      },
      start: function __do2() {
        var session = v.readSession();
        if (session.listening) {
          return unit;
        }
        ;
        return v.send(Toggle.value)();
      },
      stop: function __do2() {
        var session = v.readSession();
        if (session.listening) {
          return v.send(Toggle.value)();
        }
        ;
        return unit;
      },
      getTotalWords: function __do2() {
        var session = v.readSession();
        return session.totalWords;
      },
      getListening: function __do2() {
        var session = v.readSession();
        return session.listening;
      },
      getVersion: pure2(v.version)
    });
  };

  // output/WordMeter.Main/index.js
  var $runtime_lazy2 = function(name, moduleName, init) {
    var state = 0;
    var val;
    return function(lineNumber) {
      if (state === 2) return val;
      if (state === 1) throw new ReferenceError(name + " was needed before it finished initializing (module " + moduleName + ", line " + lineNumber + ")", moduleName, lineNumber);
      state = 1;
      val = init();
      state = 2;
      return val;
    };
  };
  var hostElementId = "word-meter";
  var main = function __do() {
    var sessionCell = $$new2(initialSession)();
    var send = function(action) {
      return function __do2() {
        modify3(reduce(action))(sessionCell)();
        return $lazy_rerender(27)();
      };
    };
    var $lazy_rerender = $runtime_lazy2("rerender", "WordMeter.Main", function() {
      return function __do2() {
        var session = read3(sessionCell)();
        return mount(hostElementId)(view(send)(session))();
      };
    });
    var rerender = $lazy_rerender(19);
    rerender();
    return install({
      send,
      readSession: read3(sessionCell),
      version
    })();
  };

  // <stdin>
  main();
})();
