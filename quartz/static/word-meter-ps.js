/* Generated by Spago v1.0.4 */
(() => {
  // output/Data.Unit/foreign.js
  var unit = void 0;

  // output/WordMeter.FFI/foreign.js
  var getElementByIdImpl = (just) => (nothing) => (id) => () => {
    const element = typeof document === "undefined" ? null : document.getElementById(id);
    return element === null ? nothing : just(element);
  };
  var setInnerHtml = (element) => (html) => () => {
    element.innerHTML = html;
  };

  // output/WordMeter.FFI/index.js
  var Nothing = /* @__PURE__ */ function() {
    function Nothing2() {
    }
    ;
    Nothing2.value = new Nothing2();
    return Nothing2;
  }();
  var Just = /* @__PURE__ */ function() {
    function Just2(value0) {
      this.value0 = value0;
    }
    ;
    Just2.create = function(value0) {
      return new Just2(value0);
    };
    return Just2;
  }();
  var getElementById = /* @__PURE__ */ function() {
    return getElementByIdImpl(Just.create)(Nothing.value);
  }();

  // output/WordMeter.Version/index.js
  var version = "0.0.1";

  // output/WordMeter.Main/index.js
  var placeholderHtml = /* @__PURE__ */ function() {
    return '<div data-testid="wm-root" style="font-family:system-ui,-apple-system,sans-serif;padding:16px;border-radius:12px;background:#0b1220;color:#e6edf3;">' + ('<div data-testid="wm-impl" style="font-size:12px;letter-spacing:0.08em;text-transform:uppercase;color:#7aa2f7;">PureScript build</div>' + ('<div data-testid="wm-count" style="font-size:48px;font-variant-numeric:tabular-nums;margin:8px 0;">0</div>' + ('<div data-testid="wm-count-label" style="font-size:14px;color:#9aa5b1;">words counted</div>' + ('<button data-testid="wm-toggle" type="button" disabled style="margin-top:12px;padding:8px 16px;border:0;border-radius:8px;background:#1f6feb;color:white;cursor:not-allowed;opacity:0.6;">Start counting</button>' + ('<div data-testid="wm-version" style="margin-top:12px;font-size:11px;color:#7d8590;">Word Meter (PureScript) v' + (version + "</div></div>"))))));
  }();
  var hostElementId = "word-meter";
  var main = function __do() {
    var hostMaybe = getElementById(hostElementId)();
    if (hostMaybe instanceof Nothing) {
      return unit;
    }
    ;
    if (hostMaybe instanceof Just) {
      return setInnerHtml(hostMaybe.value0)(placeholderHtml)();
    }
    ;
    throw new Error("Failed pattern match at WordMeter.Main (line 36, column 3 - line 38, column 51): " + [hostMaybe.constructor.name]);
  };

  // <stdin>
  main();
})();
